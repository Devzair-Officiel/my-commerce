---
# Production environment override
services:
  php:
    build:
      context: .
      target: frankenphp_prod
    env_file:
      - path: .env.prod.local
        required: false
    environment:
      APP_ENV: prod
      SERVER_NAME: "nidemiel.com"
    volumes:
      - uploads_data:/app/public/assets/images
      - videos_data:/app/public/assets/videos

  messenger-worker:
    env_file:
      - path: .env.prod.local
        required: false
    environment:
      APP_ENV: prod

  database:
    ports:
      - "127.0.0.1:5432:5432"
    env_file:
      - .env
      - path: .env.prod.local
        required: false

  netdata:
    image: netdata/netdata:stable
    container_name: netdata
    pid: host
    network_mode: host
    restart: unless-stopped
    cap_add:
      - SYS_PTRACE
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    volumes:
      - netdataconfig:/etc/netdata
      - netdatalib:/var/lib/netdata
      - netdatacache:/var/cache/netdata
      - /:/host/root:ro,rslave
      - /etc/passwd:/host/etc/passwd:ro
      - /etc/group:/host/etc/group:ro
      - /etc/localtime:/etc/localtime:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /etc/os-release:/host/etc/os-release:ro
      - /var/log:/host/var/log:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /run/dbus:/run/dbus:ro
    environment:
      - NETDATA_CLAIM_TOKEN=-j_qSFy2C3Vu2l1IokNksg7ESq8Kv1OqSKaFYMVj9YTNRN1VokEkN4Hikeue-gDKk4PuVcHZv6NdQvLNoo_nCfaVVQOYkUv0WcGaIcu4HA9gK_nW3sC-CurDhZ5uRoVzaFmMTw8
      - NETDATA_CLAIM_URL=https://app.netdata.cloud
      - NETDATA_CLAIM_ROOMS=e6716ca7-8ba0-471e-b448-c840fb0befa9

volumes:
  uploads_data:
  videos_data:
  netdataconfig:
  netdatalib:
  netdatacache:
