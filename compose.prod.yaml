---
# Production environment override
services:
  php:
    build:
      context: .
      target: frankenphp_prod
    environment:
      APP_SECRET: ${APP_SECRET}

  database:
    env_file:
      - .env
      - path: .env.prod.local
        required: false
